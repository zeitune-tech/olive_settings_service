spring:
    application:
        name: olive_insurance
    datasource:
        url: jdbc:postgresql://localhost:5432/olive_insurance
        username: lhacksrt
        password: lhacksrt
    jpa:
        hibernate:
            ddl-auto: create-drop
        show-sql: false
        properties:
            hibernate:
                format_sql: true
                dialect: org.hibernate.dialect.PostgreSQLDialect

server:
    port: ${APP_PORT:8080}
    servlet:
        context-path: /api/v1/
    error:
        include-message: always
        include-binding-errors: always

logging:
    level:
        org.springframework.orm.jpa: DEBUG
        org.springframework.security: DEBUG
        sn.lhacksrt.mpssarl: DEBUG

security:
    jwt:
        user-secret-key: ${JWT_USER_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
        user-expiration: ${JWT_USER_EXPIRATION:86400000}
        admin-secret-key: ${JWT_ADMIN_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5978}
        admin-expiration: ${JWT_ADMIN_EXPIRATION:86400000}
        refresh-token:
            expiration: ${JWT_REFRESH_EXPIRATION:604800000}

springdoc:
    default-produces-media-type: application/json
